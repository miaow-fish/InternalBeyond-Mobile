# Native iOS integration

This folder is reserved for the iOS wrapper and its bridge to the web interface.

The native layer owns platform-only capabilities such as secure credential storage and browser sign-in callbacks. The web layer communicates with it through a small message bridge.
